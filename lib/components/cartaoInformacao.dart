import 'package:aulinha_ne_pai/components/avatar.dart';
import 'package:flutter/material.dart';

class CartaoInformacao extends StatelessWidget {
  const CartaoInformacao({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(60),
      child: Column(
        children: const [
          Text('BORAAAA'),
          Text('BILLL')
        ],
      ),
    );
  }
}
